const state = () => ({
  sortOrder: 'character-name'
})

const mutations = {};

const actions = {};

const getters = {};

export default {
  namespace: true,
  state,
  mutations,
  actions,
  getters
}
